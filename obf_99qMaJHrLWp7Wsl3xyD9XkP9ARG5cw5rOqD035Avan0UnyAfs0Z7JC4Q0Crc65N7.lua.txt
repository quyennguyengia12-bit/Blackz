--// =====================================================================
--// STEAL AN EGG ULTIMATE v13.4 | FIX LOADING ORDER
--// =====================================================================

-- ===================== SERVICE =====================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local CoreGui = game:GetService("CoreGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")

local LP = Players.LocalPlayer
local PlayerGui = LP:WaitForChild("PlayerGui")
local Camera = Workspace.CurrentCamera

-- ===================== ANTI-KICK =====================
local function AntiKickFix()
    pcall(function()
        if LP and LP.Kick then
            LP.Kick = function(self, message)
                warn("⚠️ Bị kick: " .. (message or "Unknown"))
                return false
            end
        end
    end)
    
    pcall(function()
        LP.PlayerRemoving:Connect(function()
            warn("⚠️ Phát hiện ngắt kết nối, đang cố gắng giữ lại...")
            task.wait(0.5)
            if LP and LP.Character then
                warn("✅ Đã giữ được kết nối!")
            end
        end)
    end)
end

-- ===================== BIẾN TOÀN CỤC =====================
local Char, HRP, Hum
local FlyBodyVelocity = nil
local FlyUpInput, FlyDownInput = false, false
local eggCache = {}
local facilityCache = {}
local lastUpdate = 0
local KeyVerified = false
local MenuLoaded = false
local lastSafeClick = 0
local clickCooldown = 0.3
local lastTeleportTime = 0
local CORRECT_KEY = "123456789e"

-- ===================== HÀM LẤY NHÂN VẬT =====================
local function GetChar()
    Char = LP.Character or LP.CharacterAdded:Wait()
    if Char then
        HRP = Char:FindFirstChild("HumanoidRootPart")
        Hum = Char:FindFirstChild("Humanoid")
    end
end

-- ===================== TẠO GUI =====================
local function CreateSafeScreenGui(name)
    local gui = Instance.new("ScreenGui")
    gui.Name = name
    gui.ResetOnSpawn = false
    gui.IgnoreGuiInset = true
    gui.DisplayOrder = 999
    gui.Parent = CoreGui
    if not gui.Parent then gui.Parent = PlayerGui end
    return gui
end

-- ===================== DATABASE =====================
local EGG_DATABASE = {
    -- Forest
    ["Chicken"] = {value = 1, rarity = "Common", biome = "Forest"},
    ["Dog"] = {value = 2, rarity = "Common", biome = "Forest"},
    ["Bird"] = {value = 8, rarity = "Uncommon", biome = "Forest"},
    ["Owl"] = {value = 35, rarity = "Rare", biome = "Forest"},
    ["Raccoon"] = {value = 45, rarity = "Rare", biome = "Forest"},
    ["Fox"] = {value = 180, rarity = "Epic", biome = "Forest"},
    ["Bear"] = {value = 240, rarity = "Epic", biome = "Forest"},
    ["Brr Brr Patapim"] = {value = 1800, rarity = "Legendary", biome = "Forest"},
    -- Lake
    ["Frog"] = {value = 3, rarity = "Common", biome = "Lake"},
    ["Duckling"] = {value = 4, rarity = "Common", biome = "Lake"},
    ["Catfish"] = {value = 12, rarity = "Uncommon", biome = "Lake"},
    ["Turtle"] = {value = 60, rarity = "Rare", biome = "Lake"},
    ["Trulimero Trulicina"] = {value = 260, rarity = "Epic", biome = "Lake"},
    ["Swan"] = {value = 320, rarity = "Epic", biome = "Lake"},
    ["Axolotl"] = {value = 2800, rarity = "Legendary", biome = "Lake"},
    ["Leviathan"] = {value = 220000, rarity = "Cosmic", biome = "Lake"},
    -- Desert
    ["Jerboa"] = {value = 6, rarity = "Common", biome = "Desert"},
    ["Fennec"] = {value = 18, rarity = "Uncommon", biome = "Desert"},
    ["Camel"] = {value = 75, rarity = "Rare", biome = "Desert"},
    ["Tob Tobi Tob Tob"] = {value = 325, rarity = "Epic", biome = "Desert"},
    ["Snake"] = {value = 3600, rarity = "Legendary", biome = "Desert"},
    ["Sand Spider"] = {value = 16000, rarity = "Mythic", biome = "Desert"},
    ["Scorpion"] = {value = 18500, rarity = "Mythic", biome = "Desert"},
    ["Royal Sphinx"] = {value = 280000, rarity = "Cosmic", biome = "Desert"},
    -- Jungle
    ["Chimpanzee"] = {value = 90, rarity = "Rare", biome = "Jungle"},
    ["Toucan"] = {value = 110, rarity = "Rare", biome = "Jungle"},
    ["Crocodile"] = {value = 420, rarity = "Epic", biome = "Jungle"},
    ["Gorilla"] = {value = 4800, rarity = "Legendary", biome = "Jungle"},
    ["Orangutini Ananassini"] = {value = 5500, rarity = "Legendary", biome = "Jungle"},
    ["Spider"] = {value = 22000, rarity = "Mythic", biome = "Jungle"},
    ["Tiger"] = {value = 28000, rarity = "Mythic", biome = "Jungle"},
    ["King Snake"] = {value = 3500000, rarity = "Secret", biome = "Jungle"},
    -- Snow
    ["Penguin"] = {value = 140, rarity = "Rare", biome = "Snow"},
    ["Walrus"] = {value = 600, rarity = "Epic", biome = "Snow"},
    ["Polar Bear"] = {value = 7000, rarity = "Legendary", biome = "Snow"},
    ["Sabertooth Tiger"] = {value = 35000, rarity = "Mythic", biome = "Snow"},
    ["Mammoth"] = {value = 42000, rarity = "Mythic", biome = "Snow"},
    ["King Mammoth"] = {value = 400000, rarity = "Cosmic", biome = "Snow"},
    ["Yeti"] = {value = 8000000, rarity = "Secret", biome = "Snow"},
    ["Ice Dragon"] = {value = 65000000, rarity = "Eternal", biome = "Snow"},
    -- Volcano
    ["Lava Gecko"] = {value = 200, rarity = "Rare", biome = "Volcano"},
    ["Lava Frog"] = {value = 850, rarity = "Epic", biome = "Volcano"},
    ["Flaming Bull"] = {value = 9500, rarity = "Legendary", biome = "Volcano"},
    ["Lava Iguana"] = {value = 11000, rarity = "Legendary", biome = "Volcano"},
    ["Chillin Chilli"] = {value = 55000, rarity = "Mythic", biome = "Volcano"},
    ["Cerberus"] = {value = 8000000, rarity = "Secret", biome = "Volcano"},
    ["Phoenix"] = {value = 85000000, rarity = "Eternal", biome = "Volcano"},
    ["Lava Dragon"] = {value = 100000000, rarity = "Eternal", biome = "Volcano"},
    -- Abyss Ocean
    ["Parrotfish"] = {value = 220, rarity = "Rare", biome = "Abyss Ocean"},
    ["Swordfish"] = {value = 1100, rarity = "Epic", biome = "Abyss Ocean"},
    ["Shark"] = {value = 15000, rarity = "Legendary", biome = "Abyss Ocean"},
    ["Orca"] = {value = 80000, rarity = "Mythic", biome = "Abyss Ocean"},
    ["Whale Shark"] = {value = 700000, rarity = "Cosmic", biome = "Abyss Ocean"},
    ["Beluga Whale"] = {value = 850000, rarity = "Cosmic", biome = "Abyss Ocean"},
    ["Kraken"] = {value = 50000000, rarity = "Eternal", biome = "Abyss Ocean"},
    ["Mosasaurus"] = {value = 180000000, rarity = "Eternal", biome = "Abyss Ocean"},
    -- Prehistoric
    ["Dodo"] = {value = 30000, rarity = "Mythic", biome = "Prehistoric"},
    ["Pterodactyl"] = {value = 50000, rarity = "Mythic", biome = "Prehistoric"},
    ["Ankylosaurus"] = {value = 120000, rarity = "Mythic", biome = "Prehistoric"},
    ["Triceratops"] = {value = 300000, rarity = "Cosmic", biome = "Prehistoric"},
    ["Bronto"] = {value = 1500000, rarity = "Cosmic", biome = "Prehistoric"},
    ["Tralaledon"] = {value = 8000000, rarity = "Secret", biome = "Prehistoric"},
    ["TRex"] = {value = 50000000, rarity = "Eternal", biome = "Prehistoric"},
    ["Mosasaurus Prehistoric"] = {value = 200000000, rarity = "Eternal", biome = "Prehistoric"},
    -- Cosmic
    ["Unicorn"] = {value = 1000000000, rarity = "Divine", biome = "Cosmic"},
    ["Kitsune"] = {value = 1800000000, rarity = "Divine", biome = "Cosmic"},
    ["Eternal Lunar Dragon"] = {value = 250000000, rarity = "Eternal", biome = "Cosmic"},
    ["Cosmic Dragon"] = {value = 100000000, rarity = "Eternal", biome = "Cosmic"},
    -- Sakura Blossom
    ["Crane"] = {value = 50000, rarity = "Epic", biome = "Sakura Blossom"},
    ["Salamander"] = {value = 100000, rarity = "Legendary", biome = "Sakura Blossom"},
    ["Red Panda"] = {value = 500000, rarity = "Mythic", biome = "Sakura Blossom"},
    ["Snowy Owl"] = {value = 2000000, rarity = "Cosmic", biome = "Sakura Blossom"},
    ["Koi"] = {value = 5000000, rarity = "Secret", biome = "Sakura Blossom"},
    ["Stag"] = {value = 10000000, rarity = "Secret", biome = "Sakura Blossom"},
    ["Oni Tiger"] = {value = 600000000, rarity = "Eternal", biome = "Sakura Blossom"},
    ["Kitsune Sakura"] = {value = 2000000000, rarity = "Divine", biome = "Sakura Blossom"},
    -- Titan Temple
    ["Titan Gorilla"] = {value = 150000000, rarity = "Eternal", biome = "Titan Temple"},
    ["Titan Guardian"] = {value = 250000000, rarity = "Eternal", biome = "Titan Temple"},
    ["Titan King"] = {value = 500000000, rarity = "Eternal", biome = "Titan Temple"},
    ["Titan Dragon"] = {value = 1000000000, rarity = "Divine", biome = "Titan Temple"},
    -- Mutation
    ["Bloom"] = {value = 5000000, rarity = "Secret", biome = "Mutation"},
    ["Spirit Bloom"] = {value = 10000000, rarity = "Secret", biome = "Mutation"},
    ["Cosmic Bloom"] = {value = 50000000, rarity = "Cosmic", biome = "Mutation"},
    -- Brainrot
    ["Brainrot Rare"] = {value = 10000, rarity = "Rare", biome = "Brainrot"},
    ["Brainrot Epic"] = {value = 40000, rarity = "Epic", biome = "Brainrot"},
    ["Brainrot Mythic"] = {value = 4000000, rarity = "Mythic", biome = "Brainrot"},
    ["Brainrot Cosmic"] = {value = 80000000, rarity = "Cosmic", biome = "Brainrot"},
    ["Brainrot Secret"] = {value = 2000000000, rarity = "Secret", biome = "Brainrot"},
    ["Brainrot Eternal"] = {value = 11000000000, rarity = "Eternal", biome = "Brainrot"},
}

-- ===================== CẤU HÌNH =====================
local CONFIG = {
    FlySpeed = 80,
    WalkSpeed = 25,
    StealDelay = 1.5,
    HatchDelay = 1.5,
    SellInterval = 8,
    UpgradeInterval = 8,
    ClaimInterval = 8,
    RebirthInterval = 120,
    AFKInterval = 45,
    MinValueToSteal = 0,
    AutoTP = true,
    TPHeightOffset = 5,
    SelectedBiome = "Forest",
    MaxClickPerSecond = 2,
}

local State = {
    AutoSteal = false,
    AutoHatch = false,
    AutoSell = false,
    AutoUpgrade = false,
    AutoClaim = false,
    AutoRebirth = false,
    AutoFarm = false,
    AutoTreadmill = false,
    AutoPlace = false,
    Fly = false,
    SpeedBypass = false,
    WalkWater = false,
    InfiniteJump = false,
    Noclip = false,
    AntiAFK = false,
    RareHunter = false,
    MutationPriority = false,
    AutoFuse = false,
    AutoEquipBest = false,
    AutoTP = true,
    Godmode = false,
    ESP = false,
    Fullbright = false,
}

-- ===================== HÀM TIỆN ÍCH =====================
local function getEggName(egg)
    if egg:GetAttribute("Type") then return tostring(egg:GetAttribute("Type")) end
    if egg:GetAttribute("Name") then return tostring(egg:GetAttribute("Name")) end
    if egg.Parent and egg.Parent ~= Workspace and egg.Parent.Name ~= "Eggs" then return egg.Parent.Name end
    return egg.Name
end

local function isValidEgg(egg)
    if not egg or not egg.Parent then return false end
    if egg:IsA("BasePart") and (egg.Transparency == 1 or not egg.CanCollide) then return false end
    if egg:GetAttribute("Collected") or egg:GetAttribute("Stolen") or egg:GetAttribute("Claimed") then return false end
    return true
end

local function getEggValue(egg)
    local name = getEggName(egg)
    return EGG_DATABASE[name] and EGG_DATABASE[name].value or 0
end

local clickCount = 0
local clickResetTime = 0

local function canClick()
    local now = tick()
    if now - clickResetTime > 1 then
        clickCount = 0
        clickResetTime = now
    end
    if clickCount < CONFIG.MaxClickPerSecond then
        clickCount = clickCount + 1
        return true
    end
    return false
end

local function safeClick(obj)
    if not obj or not canClick() then return end
    local now = tick()
    if now - lastSafeClick < clickCooldown then return end
    lastSafeClick = now
    
    pcall(function()
        local cd = obj:FindFirstChildWhichIsA("ClickDetector")
        if cd then
            if fireclickdetector then fireclickdetector(cd) else cd:Click() end
            return
        end
        for _, child in ipairs(obj:GetDescendants()) do
            if child:IsA("ClickDetector") then
                if fireclickdetector then fireclickdetector(child) else child:Click() end
                return
            end
        end
    end)
end

local function teleportTo(pos)
    if not HRP then return end
    local now = tick()
    if now - lastTeleportTime < 1 then return end
    lastTeleportTime = now
    if (HRP.Position - pos).Magnitude < 5 then return end
    HRP.CFrame = CFrame.new(pos + Vector3.new(0, CONFIG.TPHeightOffset, 0))
end

-- ===================== CACHE =====================
local function updateCaches()
    if tick() - lastUpdate < 5 then return end
    lastUpdate = tick()
    
    eggCache = {}
    facilityCache = {}
    
    for _, obj in ipairs(Workspace:GetDescendants()) do
        if obj:IsA("BasePart") or obj:IsA("Model") then
            local nameLower = obj.Name:lower()
            if (nameLower:find("egg") or obj:GetAttribute("Egg")) and isValidEgg(obj) then
                table.insert(eggCache, obj)
            elseif nameLower:find("sell") then
                table.insert(facilityCache, {obj = obj, type = "sell"})
            elseif nameLower:find("hatch") or nameLower:find("nest") or nameLower:find("incubator") then
                table.insert(facilityCache, {obj = obj, type = "hatch"})
            elseif nameLower:find("upgrade") then
                table.insert(facilityCache, {obj = obj, type = "upgrade"})
            elseif nameLower:find("claim") or nameLower:find("reward") or nameLower:find("collect") then
                table.insert(facilityCache, {obj = obj, type = "claim"})
            elseif nameLower:find("rebirth") or nameLower:find("prestige") then
                table.insert(facilityCache, {obj = obj, type = "rebirth"})
            elseif nameLower:find("treadmill") then
                table.insert(facilityCache, {obj = obj, type = "treadmill"})
            elseif nameLower:find("place") then
                table.insert(facilityCache, {obj = obj, type = "place"})
            elseif nameLower:find("fuse") or nameLower:find("combine") or nameLower:find("merge") then
                table.insert(facilityCache, {obj = obj, type = "fuse"})
            end
        end
    end
end

local function findFacility(facType)
    for _, f in ipairs(facilityCache) do
        if f.type == facType then return f.obj end
    end
    return nil
end

-- ===================== FIND BEST EGG =====================
local function findBestEggInBiome(biome)
    local best, bestScore = nil, -1
    
    for _, egg in ipairs(eggCache) do
        if isValidEgg(egg) then
            local name = getEggName(egg)
            local data = EGG_DATABASE[name]
            
            if data and data.biome == biome then
                local score = data.value or 0
                
                if State.RareHunter then
                    local rarityBonus = {
                        ["Common"] = 1, ["Uncommon"] = 2, ["Rare"] = 5,
                        ["Epic"] = 10, ["Legendary"] = 25, ["Mythic"] = 50,
                        ["Secret"] = 100, ["Cosmic"] = 250, ["Eternal"] = 500,
                        ["Divine"] = 1000,
                    }
                    score = score * (rarityBonus[data.rarity] or 1)
                end
                
                if score > bestScore and score >= CONFIG.MinValueToSteal then
                    bestScore = score
                    best = egg
                end
            end
        end
    end
    return best
end

-- ===================== FLY SYSTEM =====================
local function updateFly()
    if not State.Fly then
        if FlyBodyVelocity then FlyBodyVelocity:Destroy(); FlyBodyVelocity = nil end
        if Hum then Hum.PlatformStand = false; Hum.AutoRotate = true end
        return
    end
    if not HRP then return end
    if not FlyBodyVelocity then
        FlyBodyVelocity = Instance.new("BodyVelocity")
        FlyBodyVelocity.MaxForce = Vector3.new(1e6, 1e6, 1e6)
        FlyBodyVelocity.P = 1e5
        FlyBodyVelocity.Parent = HRP
    end
    if Hum then Hum.PlatformStand = true; Hum.AutoRotate = false end
    
    local moveDir = Hum.MoveDirection
    local vertical = (FlyUpInput and 1 or 0) - (FlyDownInput and 1 or 0)
    local direction = Vector3.new(moveDir.X, vertical, moveDir.Z)
    FlyBodyVelocity.Velocity = direction.Magnitude > 0 and direction.Unit * CONFIG.FlySpeed or Vector3.zero
end

-- ===================== FLY CONTROLS =====================
local FlyControlsGui = nil
local function CreateFlyControls()
    if FlyControlsGui then return end
    FlyControlsGui = CreateSafeScreenGui("SE_Fly")
    FlyControlsGui.Enabled = false
    
    local function makeBtn(text, posY)
        local btn = Instance.new("TextButton", FlyControlsGui)
        btn.Size = UDim2.new(0, 40, 0, 40)
        btn.Position = UDim2.new(1, -50, posY, 0)
        btn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        btn.BackgroundTransparency = 0.3
        btn.Text = text
        btn.TextColor3 = Color3.fromRGB(0, 255, 150)
        btn.TextSize = 20
        btn.Font = Enum.Font.GothamBold
        btn.BorderSizePixel = 0
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)
        Instance.new("UIStroke", btn).Color = Color3.fromRGB(0, 255, 150)
        return btn
    end
    
    local UpBtn = makeBtn("⬆", 0.6)
    local DownBtn = makeBtn("⬇", 0.75)
    
    UpBtn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
            FlyUpInput = true
        end
    end)
    UpBtn.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
            FlyUpInput = false
        end
    end)
    DownBtn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
            FlyDownInput = true
        end
    end)
    DownBtn.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
            FlyDownInput = false
        end
    end)
    
    RunService.Heartbeat:Connect(function()
        if FlyControlsGui then FlyControlsGui.Enabled = State.Fly end
    end)
end

-- ===================== ESP =====================
local ESPContainer = nil

local function updateESP()
    if not State.ESP then
        if ESPContainer then ESPContainer:Destroy(); ESPContainer = nil end
        return
    end
    
    if not ESPContainer then
        ESPContainer = Instance.new("Folder")
        ESPContainer.Name = "SE_ESP"
        ESPContainer.Parent = CoreGui
    end
    
    for _, tag in ipairs(ESPContainer:GetChildren()) do tag:Destroy() end
    if not HRP then return end
    
    local nearby = {}
    for _, egg in ipairs(eggCache) do
        if isValidEgg(egg) then
            local pos = egg:IsA("Model") and egg:GetPivot().Position or egg.Position
            local dist = (HRP.Position - pos).Magnitude
            if dist < 400 then
                local name = getEggName(egg)
                local data = EGG_DATABASE[name]
                local val = data and data.value or 0
                local rarity = data and data.rarity or "Unknown"
                table.insert(nearby, {egg = egg, val = val, rarity = rarity, name = name})
            end
        end
    end
    
    table.sort(nearby, function(a, b) return a.val > b.val end)
    for i = 1, math.min(15, #nearby) do
        local data = nearby[i]
        local egg = data.egg
        local tag = Instance.new("BillboardGui")
        tag.Adornee = egg
        tag.Size = UDim2.new(0, 160, 0, 40)
        tag.AlwaysOnTop = true
        tag.Parent = ESPContainer
        
        local label = Instance.new("TextLabel", tag)
        label.Size = UDim2.new(1, 0, 1, 0)
        label.BackgroundTransparency = 1
        label.Text = "🥚 " .. data.name .. "\n💰 $" .. tostring(data.val) .. " | " .. data.rarity
        
        if data.val > 100000000 then
            label.TextColor3 = Color3.fromRGB(255, 215, 0)
        elseif data.val > 10000000 then
            label.TextColor3 = Color3.fromRGB(255, 150, 0)
        else
            label.TextColor3 = Color3.fromRGB(0, 255, 150)
        end
        label.TextSize = 10
        label.Font = Enum.Font.GothamBold
        label.TextStrokeTransparency = 0
        label.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    end
end

-- ===================== MAIN LOOP =====================
local lastAction = { steal = 0, hatch = 0, sell = 0, upgrade = 0, claim = 0, rebirth = 0, afk = 0, treadmill = 0, place = 0, fuse = 0 }

local function StartMainLoop()
    RunService.Heartbeat:Connect(function(dt)
        if not HRP or not Hum then return end
        local now = tick()
        
        updateCaches()
        updateFly()
        updateESP()
        
        if State.SpeedBypass then Hum.WalkSpeed = 100 else Hum.WalkSpeed = CONFIG.WalkSpeed end
        if State.InfiniteJump then Hum.JumpPower = 100 end
        
        if Char and State.Noclip then
            for _, part in ipairs(Char:GetDescendants()) do
                if part:IsA("BasePart") then part.CanCollide = false end
            end
        end
        
        if State.WalkWater and HRP then
            local pos = HRP.Position
            if pos.Y < 0 then HRP.CFrame = CFrame.new(pos.X, 5, pos.Z) end
        end
        
        if State.Godmode then
            pcall(function()
                if Hum.Health < Hum.MaxHealth then Hum.Health = Hum.MaxHealth end
            end)
        end
        
        if State.Fullbright then
            pcall(function() Lighting.Brightness = 2; Lighting.GlobalShadows = false end)
        else
            pcall(function() Lighting.Brightness = 1; Lighting.GlobalShadows = true end)
        end
        
        -- Auto Steal
        if State.AutoSteal and now - lastAction.steal > CONFIG.StealDelay then
            lastAction.steal = now
            task.spawn(function()
                local target = findBestEggInBiome(CONFIG.SelectedBiome)
                if target then
                    if State.AutoTP then
                        local pos = target:IsA("Model") and target:GetPivot().Position or target.Position
                        teleportTo(pos)
                    end
                    task.wait(CONFIG.StealDelay * 0.5)
                    safeClick(target)
                end
            end)
        end
        
        -- Auto Hatch
        if State.AutoHatch and now - lastAction.hatch > CONFIG.HatchDelay then
            lastAction.hatch = now
            task.spawn(function()
                local hatch = findFacility("hatch")
                if hatch then safeClick(hatch) end
            end)
        end
        
        -- Auto Sell
        if State.AutoSell and now - lastAction.sell > CONFIG.SellInterval then
            lastAction.sell = now
            task.spawn(function()
                local sell = findFacility("sell")
                if sell then safeClick(sell) end
            end)
        end
        
        -- Auto Upgrade
        if State.AutoUpgrade and now - lastAction.upgrade > CONFIG.UpgradeInterval then
            lastAction.upgrade = now
            task.spawn(function()
                local upgrade = findFacility("upgrade")
                if upgrade then safeClick(upgrade) end
            end)
        end
        
        -- Auto Claim
        if State.AutoClaim and now - lastAction.claim > CONFIG.ClaimInterval then
            lastAction.claim = now
            task.spawn(function()
                local claim = findFacility("claim")
                if claim then safeClick(claim) end
            end)
        end
        
        -- Auto Rebirth
        if State.AutoRebirth and now - lastAction.rebirth > CONFIG.RebirthInterval then
            lastAction.rebirth = now
            task.spawn(function()
                local rebirth = findFacility("rebirth")
                if rebirth then safeClick(rebirth) end
            end)
        end
        
        -- Auto Treadmill
        if State.AutoTreadmill and now - lastAction.treadmill > 30 then
            lastAction.treadmill = now
            task.spawn(function()
                local treadmill = findFacility("treadmill")
                if treadmill then safeClick(treadmill) end
            end)
        end
        
        -- Auto Place
        if State.AutoPlace and now - lastAction.place > 15 then
            lastAction.place = now
            task.spawn(function()
                local place = findFacility("place")
                if place then safeClick(place) end
            end)
        end
        
        -- Auto Fuse
        if State.AutoFuse and now - lastAction.fuse > 120 then
            lastAction.fuse = now
            task.spawn(function()
                local fuse = findFacility("fuse")
                if fuse then safeClick(fuse) end
            end)
        end
        
        -- Anti AFK
        if State.AntiAFK and now - lastAction.afk > CONFIG.AFKInterval then
            lastAction.afk = now
            if HRP then
                HRP.CFrame = CFrame.new(HRP.Position + Vector3.new(math.random(-2,2), 0, math.random(-2,2)))
            end
        end
    end)
end

-- ===================== MENU =====================
local function CreateMainMenu()
    if MenuLoaded then return end
    MenuLoaded = true
    
    local ScreenGui = CreateSafeScreenGui("SE_Menu")
    
    local ToggleBtn = Instance.new("TextButton", ScreenGui)
    ToggleBtn.Size = UDim2.new(0, 36, 0, 36)
    ToggleBtn.Position = UDim2.new(0, 10, 0, 120)
    ToggleBtn.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
    ToggleBtn.BackgroundTransparency = 0.2
    ToggleBtn.Text = "🥚"
    ToggleBtn.TextColor3 = Color3.fromRGB(0, 255, 150)
    ToggleBtn.TextSize = 18
    ToggleBtn.Font = Enum.Font.GothamBold
    ToggleBtn.AutoButtonColor = false
    Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(1, 0)
    Instance.new("UIStroke", ToggleBtn).Color = Color3.fromRGB(0, 255, 150)
    
    local MainFrame = Instance.new("Frame", ScreenGui)
    MainFrame.Size = UDim2.new(0, 0, 0, 0)
    MainFrame.Position = UDim2.new(0.5, -120, 0.5, -170)
    MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
    MainFrame.BackgroundTransparency = 0.15
    MainFrame.BorderSizePixel = 0
    MainFrame.Visible = false
    MainFrame.ClipsDescendants = true
    Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)
    Instance.new("UIStroke", MainFrame).Color = Color3.fromRGB(0, 255, 150)
    
    -- Title
    local TitleBar = Instance.new("Frame", MainFrame)
    TitleBar.Size = UDim2.new(1, 0, 0, 26)
    TitleBar.BackgroundTransparency = 1
    
    local Title = Instance.new("TextLabel", TitleBar)
    Title.Size = UDim2.new(0.7, 0, 1, 0)
    Title.Position = UDim2.new(0, 8, 0, 0)
    Title.BackgroundTransparency = 1
    Title.Text = "🥚 SE ULTIMATE v13.4"
    Title.TextColor3 = Color3.fromRGB(0, 255, 150)
    Title.TextSize = 12
    Title.Font = Enum.Font.GothamBold
    Title.TextXAlignment = Enum.TextXAlignment.Left
    
    local CloseBtn = Instance.new("TextButton", TitleBar)
    CloseBtn.Size = UDim2.new(0, 20, 0, 20)
    CloseBtn.Position = UDim2.new(1, -24, 0.5, -10)
    CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
    CloseBtn.BackgroundTransparency = 0.5
    CloseBtn.Text = "✕"
    CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    CloseBtn.TextSize = 11
    CloseBtn.Font = Enum.Font.GothamBold
    CloseBtn.BorderSizePixel = 0
    Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 4)
    CloseBtn.Activated:Connect(function()
        MainFrame:TweenSize(UDim2.new(0, 0, 0, 0), Enum.EasingDirection.Out, Enum.EasingStyle.Quad, 0.3, true)
        task.wait(0.3)
        MainFrame.Visible = false
    end)
    
    -- Tab
    local TabBar = Instance.new("Frame", MainFrame)
    TabBar.Size = UDim2.new(1, 0, 0, 24)
    TabBar.Position = UDim2.new(0, 0, 0, 26)
    TabBar.BackgroundTransparency = 1
    
    local tabs = {"⚡Auto", "🚀Move", "🛡️Misc"}
    local currentTab = 1
    
    local Content = Instance.new("ScrollingFrame", MainFrame)
    Content.Size = UDim2.new(1, -8, 1, -58)
    Content.Position = UDim2.new(0, 4, 0, 54)
    Content.BackgroundTransparency = 1
    Content.ScrollBarThickness = 2
    Content.ScrollBarImageColor3 = Color3.fromRGB(0, 255, 150)
    Content.CanvasSize = UDim2.new(0, 0, 0, 0)
    
    local ContentList = Instance.new("UIListLayout", Content)
    ContentList.SortOrder = Enum.SortOrder.LayoutOrder
    ContentList.Padding = UDim.new(0, 2)
    
    -- UI Elements
    local function MakeToggle(text, getter, setter)
        local frame = Instance.new("TextButton", Content)
        frame.Size = UDim2.new(1, 0, 0, 24)
        frame.BackgroundColor3 = Color3.fromRGB(25,25,35)
        frame.BackgroundTransparency = 0.3
        frame.BorderSizePixel = 0
        frame.Text = ""
        frame.AutoButtonColor = false
        Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 4)
        
        local label = Instance.new("TextLabel", frame)
        label.Size = UDim2.new(0.7, 0, 1, 0)
        label.Position = UDim2.new(0, 6, 0, 0)
        label.BackgroundTransparency = 1
        label.Text = text
        label.TextColor3 = Color3.fromRGB(230,230,230)
        label.TextSize = 10
        label.Font = Enum.Font.GothamMedium
        label.TextXAlignment = Enum.TextXAlignment.Left
        
        local switchBg = Instance.new("Frame", frame)
        switchBg.Size = UDim2.new(0, 20, 0, 11)
        switchBg.Position = UDim2.new(1, -24, 0.5, -5.5)
        switchBg.BackgroundColor3 = Color3.fromRGB(40,40,50)
        switchBg.BorderSizePixel = 0
        Instance.new("UICorner", switchBg).CornerRadius = UDim.new(1, 0)
        
        local dot = Instance.new("Frame", switchBg)
        dot.Size = UDim2.new(0, 7, 0, 7)
        dot.Position = UDim2.new(0, 2, 0.5, -3.5)
        dot.BackgroundColor3 = Color3.fromRGB(100,100,100)
        dot.BorderSizePixel = 0
        Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0)
        
        local state = getter()
        local function updateSwitch()
            dot.Position = state and UDim2.new(1, -9, 0.5, -3.5) or UDim2.new(0, 2, 0.5, -3.5)
            dot.BackgroundColor3 = state and Color3.fromRGB(0,255,150) or Color3.fromRGB(100,100,100)
            switchBg.BackgroundColor3 = state and Color3.fromRGB(0,80,50) or Color3.fromRGB(40,40,50)
        end
        updateSwitch()
        
        frame.Activated:Connect(function()
            state = not state
            setter(state)
            updateSwitch()
        end)
    end
    
    local function MakeSlider(text, min, max, default, callback)
        local frame = Instance.new("Frame", Content)
        frame.Size = UDim2.new(1, 0, 0, 32)
        frame.BackgroundColor3 = Color3.fromRGB(25,25,35)
        frame.BackgroundTransparency = 0.3
        frame.BorderSizePixel = 0
        Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 4)
        
        local label = Instance.new("TextLabel", frame)
        label.Size = UDim2.new(1, -8, 0, 12)
        label.Position = UDim2.new(0, 6, 0, 1)
        label.BackgroundTransparency = 1
        label.Text = text .. ": " .. default
        label.TextColor3 = Color3.fromRGB(230,230,230)
        label.TextSize = 9
        label.Font = Enum.Font.GothamMedium
        label.TextXAlignment = Enum.TextXAlignment.Left
        
        local bg = Instance.new("Frame", frame)
        bg.Size = UDim2.new(1, -12, 0, 4)
        bg.Position = UDim2.new(0, 6, 0, 20)
        bg.BackgroundColor3 = Color3.fromRGB(15,15,20)
        bg.BorderSizePixel = 0
        Instance.new("UICorner", bg).CornerRadius = UDim.new(1, 0)
        
        local fill = Instance.new("Frame", bg)
        local defaultRel = (default - min) / (max - min)
        fill.Size = UDim2.new(defaultRel, 0, 1, 0)
        fill.BackgroundColor3 = Color3.fromRGB(0,255,150)
        fill.BorderSizePixel = 0
        Instance.new("UICorner", fill).CornerRadius = UDim.new(1, 0)
        
        local thumb = Instance.new("Frame", bg)
        thumb.Size = UDim2.new(0, 8, 0, 8)
        thumb.Position = UDim2.new(defaultRel, -4, 0.5, -4)
        thumb.BackgroundColor3 = Color3.fromRGB(255,255,255)
        thumb.BorderSizePixel = 0
        Instance.new("UICorner", thumb).CornerRadius = UDim.new(1, 0)
        
        local dragging = false
        local function updateSlider(x)
            local bgAbsSize = bg.AbsoluteSize.X
            if bgAbsSize <= 0 then return end
            local relX = math.clamp((x - bg.AbsolutePosition.X) / bgAbsSize, 0, 1)
            local val = math.floor(min + (max - min) * relX)
            fill.Size = UDim2.new(relX, 0, 1, 0)
            thumb.Position = UDim2.new(relX, -4, 0.5, -4)
            label.Text = text .. ": " .. val
            callback(val)
        end
        
        thumb.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        bg.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
                updateSlider(input.Position.X)
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                updateSlider(input.Position.X)
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
    end
    
    local function MakeDropdown(text, options, getter, setter)
        local frame = Instance.new("Frame", Content)
        frame.Size = UDim2.new(1, 0, 0, 32)
        frame.BackgroundColor3 = Color3.fromRGB(25,25,35)
        frame.BackgroundTransparency = 0.3
        frame.BorderSizePixel = 0
        Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 4)
        
        local label = Instance.new("TextLabel", frame)
        label.Size = UDim2.new(0.4, 0, 1, 0)
        label.Position = UDim2.new(0, 6, 0, 0)
        label.BackgroundTransparency = 1
        label.Text = text
        label.TextColor3 = Color3.fromRGB(230,230,230)
        label.TextSize = 10
        label.Font = Enum.Font.GothamMedium
        label.TextXAlignment = Enum.TextXAlignment.Left
        
        local dropdown = Instance.new("TextButton", frame)
        dropdown.Size = UDim2.new(0.5, 0, 1, -4)
        dropdown.Position = UDim2.new(0.48, 0, 0, 2)
        dropdown.BackgroundColor3 = Color3.fromRGB(35,35,45)
        dropdown.BorderSizePixel = 0
        dropdown.Text = getter()
        dropdown.TextColor3 = Color3.fromRGB(0,255,150)
        dropdown.TextSize = 10
        dropdown.Font = Enum.Font.GothamMedium
        Instance.new("UICorner", dropdown).CornerRadius = UDim.new(0, 4)
        
        local list = Instance.new("Frame", frame)
        list.Size = UDim2.new(0.5, 0, 0, 0)
        list.Position = UDim2.new(0.48, 0, 0, 28)
        list.BackgroundColor3 = Color3.fromRGB(25,25,35)
        list.BackgroundTransparency = 0.3
        list.BorderSizePixel = 0
        list.ClipsDescendants = true
        list.Visible = false
        Instance.new("UICorner", list).CornerRadius = UDim.new(0, 4)
        
        local listLayout = Instance.new("UIListLayout", list)
        listLayout.SortOrder = Enum.SortOrder.LayoutOrder
        listLayout.Padding = UDim.new(0, 2)
        
        for _, opt in ipairs(options) do
            local btn = Instance.new("TextButton", list)
            btn.Size = UDim2.new(1, 0, 0, 24)
            btn.BackgroundColor3 = Color3.fromRGB(30,30,40)
            btn.BorderSizePixel = 0
            btn.Text = opt
            btn.TextColor3 = Color3.fromRGB(200,200,200)
            btn.TextSize = 10
            btn.Font = Enum.Font.GothamMedium
            Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 3)
            
            btn.Activated:Connect(function()
                setter(opt)
                dropdown.Text = opt
                list.Visible = false
                list.Size = UDim2.new(0.5, 0, 0, 0)
            end)
        end
        
        dropdown.Activated:Connect(function()
            list.Visible = not list.Visible
            if list.Visible then
                local count = #options
                list.Size = UDim2.new(0.5, 0, 0, count * 26 + 4)
            else
                list.Size = UDim2.new(0.5, 0, 0, 0)
            end
        end)
    end
    
    -- Build tabs
    local function clearContent()
        for _, child in ipairs(Content:GetChildren()) do
            if child:IsA("Frame") or child:IsA("TextButton") then
                child:Destroy()
            end
        end
    end
    
    local function updateCanvas()
        local count = 0
        for _, child in ipairs(Content:GetChildren()) do
            if child:IsA("Frame") or child:IsA("TextButton") then
                count = count + 1
            end
        end
        Content.CanvasSize = UDim2.new(0, 0, 0, count * 26 + 10)
    end
    
    local function buildTab1()
        clearContent()
        MakeToggle("🥚 Auto Steal", function() return State.AutoSteal end, function(v) State.AutoSteal = v end)
        MakeToggle("🐣 Auto Hatch", function() return State.AutoHatch end, function(v) State.AutoHatch = v end)
        MakeToggle("💰 Auto Sell", function() return State.AutoSell end, function(v) State.AutoSell = v end)
        MakeToggle("⬆️ Auto Upgrade", function() return State.AutoUpgrade end, function(v) State.AutoUpgrade = v end)
        MakeToggle("🏃 Auto Treadmill", function() return State.AutoTreadmill end, function(v) State.AutoTreadmill = v end)
        MakeToggle("🎁 Auto Claim", function() return State.AutoClaim end, function(v) State.AutoClaim = v end)
        MakeToggle("📦 Auto Place", function() return State.AutoPlace end, function(v) State.AutoPlace = v end)
        MakeToggle("🔄 Auto Rebirth", function() return State.AutoRebirth end, function(v) State.AutoRebirth = v end)
        MakeDropdown("🌍 Chọn Biome", {"Forest", "Lake", "Desert", "Jungle", "Snow", "Volcano", "Abyss Ocean", "Prehistoric", "Cosmic", "Sakura Blossom", "Titan Temple", "Mutation", "Brainrot"}, 
            function() return CONFIG.SelectedBiome end, 
            function(v) CONFIG.SelectedBiome = v end)
        MakeSlider("⏱️ Steal Delay", 0.5, 5, CONFIG.StealDelay, function(v) CONFIG.StealDelay = v end)
        MakeSlider("⏱️ Hatch Delay", 0.5, 5, CONFIG.HatchDelay, function(v) CONFIG.HatchDelay = v end)
        updateCanvas()
    end
    
    local function buildTab2()
        clearContent()
        MakeToggle("🚀 Fly", function() return State.Fly end, function(v)
            State.Fly = v
            if v then
                CreateFlyControls()
                if not FlyBodyVelocity and HRP then
                    FlyBodyVelocity = Instance.new("BodyVelocity")
                    FlyBodyVelocity.MaxForce = Vector3.new(1e6, 1e6, 1e6)
                    FlyBodyVelocity.P = 1e5
                    FlyBodyVelocity.Parent = HRP
                end
                if Hum then Hum.PlatformStand = true; Hum.AutoRotate = false end
            else
                if FlyBodyVelocity then FlyBodyVelocity:Destroy(); FlyBodyVelocity = nil end
                if Hum then Hum.PlatformStand = false; Hum.AutoRotate = true end
            end
        end)
        MakeSlider("✈️ Fly Speed", 20, 400, CONFIG.FlySpeed, function(v) CONFIG.FlySpeed = v end)
        MakeSlider("🏃 Walk Speed", 16, 300, CONFIG.WalkSpeed, function(v)
            CONFIG.WalkSpeed = v
            if Hum then Hum.WalkSpeed = v end
        end)
        MakeToggle("⚡ Speed Bypass", function() return State.SpeedBypass end, function(v) State.SpeedBypass = v end)
        MakeToggle("🌊 Walk on Water", function() return State.WalkWater end, function(v) State.WalkWater = v end)
        MakeToggle("🦘 Infinite Jump", function() return State.InfiniteJump end, function(v) State.InfiniteJump = v end)
        MakeToggle("👻 Noclip", function() return State.Noclip end, function(v) State.Noclip = v end)
        MakeToggle("⏳ Anti AFK", function() return State.AntiAFK end, function(v) State.AntiAFK = v end)
        updateCanvas()
    end
    
    local function buildTab3()
        clearContent()
        MakeToggle("🛡️ God Mode", function() return State.Godmode end, function(v) State.Godmode = v end)
        MakeToggle("👁️ ESP", function() return State.ESP end, function(v) State.ESP = v end)
        MakeToggle("🔦 Fullbright", function() return State.Fullbright end, function(v)
            State.Fullbright = v
            pcall(function() Lighting.Brightness = v and 2 or 1; Lighting.GlobalShadows = not v end)
        end)
        MakeToggle("⭐ Rare Hunter", function() return State.RareHunter end, function(v) State.RareHunter = v end)
        MakeToggle("🧬 Mutation Priority", function() return State.MutationPriority end, function(v) State.MutationPriority = v end)
        MakeToggle("🔮 Auto Fuse", function() return State.AutoFuse end, function(v) State.AutoFuse = v end)
        MakeToggle("🔄 Auto TP", function() return State.AutoTP end, function(v) State.AutoTP = v end)
        MakeSlider("🎯 Min Value", 0, 1000000, CONFIG.MinValueToSteal, function(v) CONFIG.MinValueToSteal = v end)
        updateCanvas()
    end
    
    local function updateContent()
        if currentTab == 1 then buildTab1()
        elseif currentTab == 2 then buildTab2()
        elseif currentTab == 3 then buildTab3() end
    end
    
    -- Tab buttons
    local tabBtns = {}
    for i, name in ipairs(tabs) do
        local btn = Instance.new("TextButton", TabBar)
        btn.Size = UDim2.new(1/3, 0, 1, 0)
        btn.Position = UDim2.new((i-1)/3, 0, 0, 0)
        btn.BackgroundColor3 = i==1 and Color3.fromRGB(0,50,30) or Color3.fromRGB(20,20,25)
        btn.BackgroundTransparency = 0.3
        btn.Text = name
        btn.TextColor3 = i==1 and Color3.fromRGB(0,255,150) or Color3.fromRGB(180,180,180)
        btn.TextSize = 9
        btn.Font = Enum.Font.GothamBold
        btn.BorderSizePixel = 0
        btn.AutoButtonColor = false
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 4)
        btn.Activated:Connect(function()
            currentTab = i
            for j, b in ipairs(tabBtns) do
                b.BackgroundColor3 = j==i and Color3.fromRGB(0,50,30) or Color3.fromRGB(20,20,25)
                b.TextColor3 = j==i and Color3.fromRGB(0,255,150) or Color3.fromRGB(180,180,180)
            end
            updateContent()
        end)
        tabBtns[i] = btn
    end
    
    ToggleBtn.Activated:Connect(function()
        MainFrame.Visible = not MainFrame.Visible
        if MainFrame.Visible then
            MainFrame:TweenSize(UDim2.new(0, 260, 0, 380), Enum.EasingDirection.Out, Enum.EasingStyle.Quad, 0.3, true)
            updateContent()
        else
            MainFrame:TweenSize(UDim2.new(0, 0, 0, 0), Enum.EasingDirection.Out, Enum.EasingStyle.Quad, 0.3, true)
        end
    end)
    
    updateContent()
    print("✅ SE ULTIMATE v13.4 loaded!")
end

-- ===================== KEY GUI =====================
local function CreateKeyGUI()
    local ScreenGui = CreateSafeScreenGui("SE_Key")
    
    local MainFrame = Instance.new("Frame", ScreenGui)
    MainFrame.Size = UDim2.new(0, 220, 0, 110)
    MainFrame.Position = UDim2.new(0.5, -110, 0.5, -55)
    MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
    MainFrame.BackgroundTransparency = 0.15
    MainFrame.BorderSizePixel = 0
    Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)
    Instance.new("UIStroke", MainFrame).Color = Color3.fromRGB(0, 255, 150)
    
    local Title = Instance.new("TextLabel", MainFrame)
    Title.Size = UDim2.new(1, 0, 0, 24)
    Title.BackgroundTransparency = 1
    Title.Text = "🔑 SE ULTIMATE v13.4"
    Title.TextColor3 = Color3.fromRGB(0, 255, 150)
    Title.TextSize = 13
    Title.Font = Enum.Font.GothamBold
    
    local KeyBox = Instance.new("TextBox", MainFrame)
    KeyBox.Size = UDim2.new(0.8, 0, 0, 26)
    KeyBox.Position = UDim2.new(0.1, 0, 0, 32)
    KeyBox.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    KeyBox.TextColor3 = Color3.fromRGB(255, 255, 255)
    KeyBox.TextSize = 11
    KeyBox.Font = Enum.Font.GothamMedium
    KeyBox.PlaceholderText = "Nhập Key..."
    KeyBox.ClearTextOnFocus = false
    Instance.new("UICorner", KeyBox).CornerRadius = UDim.new(0, 6)
    
    local SubmitBtn = Instance.new("TextButton", MainFrame)
    SubmitBtn.Size = UDim2.new(0.35, 0, 0, 26)
    SubmitBtn.Position = UDim2.new(0.325, 0, 0, 70)
    SubmitBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 150)
    SubmitBtn.Text = "OK"
    SubmitBtn.TextColor3 = Color3.fromRGB(10, 10, 15)
    SubmitBtn.TextSize = 11
    SubmitBtn.Font = Enum.Font.GothamBold
    Instance.new("UICorner", SubmitBtn).CornerRadius = UDim.new(0, 6)
    
    local ErrorLabel = Instance.new("TextLabel", MainFrame)
    ErrorLabel.Size = UDim2.new(1, 0, 0, 16)
    ErrorLabel.Position = UDim2.new(0, 0, 0, 96)
    ErrorLabel.BackgroundTransparency = 1
    ErrorLabel.Text = ""
    ErrorLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
    ErrorLabel.TextSize = 9
    ErrorLabel.Font = Enum.Font.GothamMedium
    
    local function submitKey()
        if KeyBox.Text == CORRECT_KEY then
            KeyVerified = true
            ScreenGui:Destroy()
            task.wait(0.3)
            CreateMainMenu()
        else
            ErrorLabel.Text = "❌ Sai Key! Nhập: " .. CORRECT_KEY
            KeyBox.Text = ""
            KeyBox:CaptureFocus()
        end
    end
    SubmitBtn.Activated:Connect(submitKey)
    KeyBox.FocusLost:Connect(function(enter) if enter then submitKey() end end)
end

-- ===================== KHỞI CHẠY =====================
GetChar()
AntiKickFix()
task.wait(1)
CreateKeyGUI()
print("🔑 SE ULTIMATE v13.4 | Key: 123456789e")

-- Khởi động main loop
task.wait(0.5)
StartMainLoop()
